# -- pow(base, exp[, mod]) --

# Returns base to the power exp
# If mod is present, return base to the power exp, modulo mod
# Parameters are NUMERIC type
# Returns a NUMERIC

pow(5, 5) # Returns: 3125

pow(10, -2) # Returns: 0.01

pow(7, 3, 8) # Returns: 7