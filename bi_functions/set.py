# -- set([iterable]) --

# Return a new SET object
# The optional iterable would be the element inside the SET

set() # Returns an empry set

numbers = [1, 2, 3, 4, 5]
set(numbers) # Returns: {1, 2, 3, 4, 5}